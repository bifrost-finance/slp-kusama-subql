{
  "name": "slp-kusama-subql",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql build",
    "prepack": "rm -rf dist && yarn build",
    "codegen": "subql codegen"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Bifrost",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^9",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "typescript": "^4.1.3"
  }
}
